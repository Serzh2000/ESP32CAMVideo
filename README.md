# ESP32-CAM Видеорегистратор с Telegram

Это проект превращает вашу плату ESP32-CAM в простую камеру видеонаблюдения, которой можно управлять через Telegram. Она умеет записывать видео на карту памяти и отправлять его вам в чат.

---

## 📦 Что нужно для работы

1. **Плата ESP32-CAM** (желательно модель AI Thinker).
2. **Карта памяти microSD** (до 32 ГБ, обязательно отформатируйте в FAT32).
3. **USB-UART адаптер** (программатор) для прошивки платы.
4. **Хороший блок питания** (5 вольт, 2 ампера). *Внимание: питания от USB компьютера может не хватить для работы камеры и WiFi!*

---

## 🛠 Установка (Пошаговая инструкция)

### Шаг 1: Подготовка Arduino IDE
1. Установите **Arduino IDE**.
2. Добавьте поддержку ESP32: `Файл` -> `Настройки` -> `Дополнительные ссылки для Менеджера плат`. Вставьте туда:
   `https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json`
3. Перейдите в `Инструменты` -> `Плата` -> `Менеджер плат`, найдите **esp32** и установите версию **3.0.0** или новее.

### Шаг 2: Установка библиотек
В Arduino IDE перейдите в `Скетч` -> `Подключить библиотеку` -> `Управление библиотеками`. Найдите и установите:
1. **UniversalTelegramBot** (автор Brian Lough).
2. **ArduinoJson** (автор Benoit Blanchon).

### Шаг 3: Создание Telegram бота
1. Откройте Telegram и найдите бота **@BotFather**.
2. Напишите ему `/newbot`.
3. Придумайте имя и юзернейм для бота.
4. В ответ вы получите **API Token** (длинная строка с цифрами и буквами). **Сохраните его!**

### Шаг 4: Настройка кода
1. Откройте файл `ESP32CAM_Telegram.ino`.
2. Найдите вкладку `Config.h`.
3. В строке `#define BOT_TOKEN "ВАШ_ТОКЕН"` замените `ВАШ_ТОКЕН` на тот, что дал BotFather.
   - Пример: `#define BOT_TOKEN "123456789:AAH...etc"`

### Шаг 5: Прошивка
1. Подключите ESP32-CAM к компьютеру через адаптер. *Не забудьте соединить пин IO0 с GND перед включением для режима прошивки!*
2. Выберите плату: `Инструменты` -> `Плата` -> `AI Thinker ESP32-CAM`.
3. Нажмите кнопку **Загрузка (Upload)** (стрелочка вправо).
4. После загрузки **отключите IO0 от GND** и нажмите кнопку Reset на плате.

---

## 🚀 Как пользоваться

### Первое включение (Настройка WiFi)
1. Вставьте карту памяти и включите питание.
2. Светодиод (вспышка) начнет **быстро мигать**. Это значит, камера не может подключиться к WiFi и создала свою точку доступа.
3. Возьмите телефон, зайдите в настройки WiFi и подключитесь к сети **ESP32-CAM-Setup** (без пароля).
4. Автоматически откроется страница настройки (или перейдите в браузере на `192.168.4.1`).
5. Введите имя вашей домашней WiFi сети и пароль. Нажмите "Сохранить".
6. Плата перезагрузится и подключится к интернету.

### Управление через Telegram
Найдите своего бота в Telegram и нажмите **Start**.
Появится меню с кнопками:

- **▶️ Начать запись** — Камера начнет записывать видео.
- **⏹ Остановить** — Остановить запись. Видео сохранится на карту и отправится вам в чат.
- **⚙ Настройки** — Меню настроек (длительность, FPS, фонарик).
- **ℹ️ Статус** — Показать свободное место на карте и текущие настройки.

### 💡 Быстрые команды
Вы можете просто отправить боту число, чтобы быстро поменять настройки:
- Отправьте число от **10 до 30** — это установит **FPS** (кадров в секунду).
- Отправьте число от **30 до 1800** — это установит **длительность записи** в секундах.
- Пример: отправьте `15` для 15 кадров/сек, или `600` для записи по 10 минут.

---

## ❓ Решение проблем

**Бот не отвечает**
- Проверьте питание. Если напряжения мало, плата зависает при включении WiFi.
- Убедитесь, что токен бота верный.
- Попробуйте отправить `/start` еще раз.

**Видео не отправляется**
- Возможно, файл слишком большой (Telegram принимает файлы до 50 МБ). Уменьшите длительность или FPS.
- Проверьте качество WiFi сигнала.

**Светодиод постоянно быстро мигает**
- Камера не может подключиться к вашему WiFi. Подключитесь к `ESP32-CAM-Setup` и введите пароль заново.

**Ошибка "Camera Init Failed"**
- Проверьте, что модуль камеры плотно вставлен в разъем.
