# ESP32-CAM Telegram Видеорегистратор

Этот проект превращает модуль ESP32-CAM в камеру видеонаблюдения, которая записывает видеоклипы (AVI/MJPEG) и отправляет их в Telegram-бот.

## Возможности
- **Запись видео**: Записывает видеоклипы в формате AVI на SD-карту (только видео, без звука).
- **Интеграция с Telegram**: Отправляет записанные видео в ваш Telegram-бот.
- **Удаленное управление**: Запуск/остановка записи, изменение настроек через команды Telegram.
- **Captive Portal (Точка доступа)**: Автоматически создает WiFi точку доступа `ESP32-CAM-Setup`, если не удается подключиться к WiFi, позволяя настроить имя сети и пароль через веб-интерфейс.
- **Управление питанием**: Отключает WiFi во время записи для экономии энергии и предотвращения сбоев (brownouts).

## Требования к оборудованию
- **ESP32-CAM** (AI Thinker или совместимая)
- **MicroSD карта** (Формат FAT32, рекомендуется макс. 32GB)
- **Блок питания 5V** (минимум 2A, питание от USB может быть недостаточным для работы WiFi + Камеры + SD-карты одновременно)
- **FTDI адаптер** (для прошивки)

## Необходимые библиотеки
Установите эти библиотеки через Менеджер библиотек Arduino или PlatformIO:
1. **UniversalTelegramBot** от Brian Lough
2. **ArduinoJson** от Benoit Blanchon
3. **ESP32 Core** (Поддержка плат)

## Настройка
1. Откройте `Config.h` (необязательно), чтобы проверить назначения пинов, если вы не используете модуль AI Thinker.
2. Откройте `Config.h` и убедитесь, что `BOTtoken` указан верно (или задан в `ESP32CAM_Telegram.ino`).
3. Загрузите код в плату.

## Запуск
1. Вставьте SD-карту.
2. Включите питание ESP32-CAM.
3. Если WiFi не настроен или подключение не удалось, светодиод будет быстро мигать. Подключитесь к WiFi сети `ESP32-CAM-Setup` и перейдите по адресу `192.168.4.1` (или любому другому) для настройки WiFi.
4. После подключения красный светодиод погаснет (инвертированная логика).
5. Отправьте `/start` вашему Telegram-боту для инициализации.

## Команды Telegram
- `/start` или `❓ Помощь` - Показать меню и текущие настройки.
- `/record` или `▶️ Начать запись` - Запустить непрерывный цикл записи.
- `/stop` или `⏹ Остановить` - Остановить запись.
- `/status` или `ℹ️ Статус` - Показать статус системы (Память, Настройки).
- `⏱ 30с` / `⏱ 300с` - Установить длительность записи (по умолчанию 5 минут / 300с).
- `🎞 10 FPS` / `🎞 25 FPS` - Установить частоту кадров.

## Устранение неполадок
- **Сбои / Перезагрузки / Brownout**: Почти всегда это проблема с питанием. ESP32-CAM потребляет большой ток при использовании WiFi и камеры одновременно. Убедитесь, что у вас хороший источник питания 5V 2A. Код автоматически отключает WiFi во время записи, чтобы смягчить эту проблему.
- **Ошибка SD-карты**: Убедитесь, что карта отформатирована в FAT32. Попробуйте переформатировать. Проверьте контакты.
- **Ошибка загрузки видео**: Telegram ограничивает загрузку файлов до 50 МБ. Если видео слишком большое, загрузка может не удаться. Уменьшите длительность или FPS. Код также имеет встроенное ограничение в 45 МБ.
- **Бот не отвечает**: Отправьте `/start` еще раз. Проверьте монитор порта на наличие ошибок "Connection failed".

## Структура проекта
- `ESP32CAM_Telegram.ino`: Основной скетч.
- `Config.h`: Определения пинов и константы.
- `AviUtils.h/cpp`: Вспомогательные функции для создания AVI файлов.
- `VideoRecorder.h/cpp`: Логика записи видео.
- `TelegramManager.h/cpp`: Логика общения с Telegram.
- `WifiManager.h/cpp`: Логика WiFi и Captive Portal.
